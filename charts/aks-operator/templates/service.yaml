apiVersion: v1
kind: Service
metadata:
  name: {{ include "aks-operator.fullname" . }}
  labels:
    {{- include "aks-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aks-operator.selectorLabels" . | nindent 4 }}
